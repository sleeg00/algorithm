
/*#include <algorithm>
#include <iostream>

using namespace std;

// N개 A1, A2.. AN이 주어진다.
// 연속된 부분 구간의 합이 M으로 나누어 떨어지는 구간의 개수 구하기
// 즉 구간 합 % M == 0 (i,j) 쌍 구하기 [i,j] => i~j

// 1<= N <= 1,000,000
int main(void) {
    cin.tie(NULL);
    cout.tie(NULL);
    ios_base::sync_with_stdio(false);
    int hap = 0;
    for (int i = 1; i <= 1000000; i++) {
        hap += i;
    }
    cout << hap;
}
 */